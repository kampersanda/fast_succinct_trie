# Benchmark for tries (in static case)

## Build instructions

You can download and compile this library as the following commands:

```
$ git submodule update --init --recursive
$ mkdir build && cd build
$ cmake ..
$ make
```

